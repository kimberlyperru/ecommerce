{
  "dependencies": {
    "dotenv": "^17.2.3",
    "concurrently": "^8.2.2"
  },
  "scripts": {
  "build": "cd frontend && yarn && npm run build && cd ../backend && yarn",
  "start": "concurrently \"npm start --prefix frontend\" \"npm start --prefix backend\""
}
}